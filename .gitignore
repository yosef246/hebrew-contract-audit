# secrets
.env
.env.local
*.token

# runtime data (checkpoints, telemetry) — not committed
data/*
!data/.gitkeep

# fixtures — real contracts contain PII. Only the synthetic sample + annotations are committed.
fixtures/raw/
fixtures/*.txt
!fixtures/sample-00.txt

# python
__pycache__/
*.pyc
.venv/
venv/

# node (web/)
node_modules/
.next/

# os / editor
.DS_Store
Thumbs.db
